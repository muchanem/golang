package main

import (
	"fmt"
)

func main() {
	// setting array with string called colours
	var colours [3]string
	colours[0] = "Red"
	colours[1] = "Orange"
	colours[2] = "Yellow"
	fmt.Println(colours)
}
